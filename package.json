{
  "main": "index.html",
  "scripts": {
    "start": "parcel src/index.html --https",
    "deploy": "parcel build src/index.html && cp -R dist/* ~/../../keybase/private/tino,kbpbot/my-site",
    "format": "prettier --write '**/*.js'"
  },
  "dependencies": {
    "parcel": "^1.10.3",
    "prettier": "^1.14.3"
  }
}
